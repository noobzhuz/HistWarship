# Map Interaction Improvements

Status: Deferred until the database migration is complete.

Items:

- Improve "Show on map" behavior.
- When a search result is selected, zoom closer to the target museum site.
- Automatically open the site's preview popup after the map finishes moving.
- Visually highlight the selected marker (for example: pulse animation, temporary color change, or halo).
- If multiple museum sites are close together, make it immediately obvious which marker was selected.
- Preserve existing hover preview and pinned popup behavior.
- Review zoom level after homepage and full-map migration are complete.

Note:

These items should be addressed together during the future UI/UX polish phase rather than during the current database migration.
